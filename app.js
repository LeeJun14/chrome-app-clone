/* const는 상수 -> 변하지 않는다. */
const a = 5;
const b = 2;
/* let은 const와는 다르게 중간에 값을 변경가능 */
let myName = "jun";

console. log(a + b);
console. log(a * b);
console. log(a / b);
console. log("hello " + myName);

myName = "junyeon";

console.log("your new name is " + myName);